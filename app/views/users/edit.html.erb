<%= form_for(@user, url:"/users/#{@user.id}") do |f| %>
  <h4>タイトル</h4>
  <%= f.text_field :name %>
  <h4>本文</h4>
  <%= f.text_area :introduction %>
   <h4>画像</h4>
  <%= f.attachment_field :profile_image %>
  <%= f.submit '保存' %>
<% end %>
